<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.session4">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="MainActivity"
            android:label="@string/title_activity_main3"
            android:theme="@style/AppTheme.NoActionBar"
            tools:ignore="MissingClass" />
        <activity
            android:name=".ui.appBar.Activity3"
            android:parentActivityName=".ui.appBar.MainActivity" />
        <activity
            android:name=".ui.appBar.Activity2"
            android:parentActivityName=".ui.appBar.MainActivity" />
        <activity
            android:name=".ui.appBar.Activity1"
            android:parentActivityName=".ui.appBar.MainActivity" />
        <activity
            android:name=".ui.appBar.MainActivity"
            android:parentActivityName=".ui.appBar.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>