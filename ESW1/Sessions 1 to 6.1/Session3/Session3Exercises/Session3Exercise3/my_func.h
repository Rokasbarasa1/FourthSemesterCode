#pragma once

int power(int x, int y); // A basic power fucntion that must recieve 2 parameters. For signed integers only
void multiSwap(int* x, int* y, int* z); // Swaps given values, must put in memory pointers